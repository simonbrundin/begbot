apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: begbot

resources:
  - ../../base

patches:
  - path: replicas-patch.yaml
  - path: resources-patch.yaml

commonLabels:
  component: frontend
  app: begbot-frontend

namePrefix: begbot-frontend-
