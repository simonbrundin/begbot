apiVersion: apps/v1
kind: Deployment
metadata:
  name: begbot
spec:
  template:
    spec:
      containers:
        - name: begbot
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "200m"
