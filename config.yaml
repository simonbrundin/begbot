database:
  host: "aws-1-eu-west-1.pooler.supabase.com"
  port: 5432
  user: "postgres.fxhknzpqhrkpqothjvrx"
  password: "" # Set via DATABASE_PASSWORD env var
  name: "postgres"
  sslmode: "require"

app:
  log_level: "debug"
  cache_ttl: 24h

scraping:
  tradera:
    enabled: true
    timeout: 30s
  blocket:
    enabled: true
    timeout: 30s

llm:
  provider: "openrouter"
  api_key: "" # Set via LLM_API_KEY env var
  site_url: "http://localhost:3000"
  site_name: "Begbot"
  default_model: "deepseek/deepseek-v3.2"
  models:
    ExtractProductInfo: "deepseek/deepseek-v3.2"
    ValidateProduct: "deepseek/deepseek-v3.2"
    CheckProductCondition: "deepseek/deepseek-v3.2"
    EstimateNewPrice: "deepseek/deepseek-v3.2"

valuation:
  target_sell_days: 14
  min_profit_margin: 0.15
  safety_margin: 0.2

email:
  smtp_host: "smtp.gmail.com"
  smtp_port: "587"
  smtp_username: "" # Set via SMTP_USERNAME env var
  smtp_password: "" # Set via SMTP_PASSWORD env var
  from: "" # Set via SMTP_FROM env var
  recipients:
    - "SMTP_FROM"
    # - "freddie_tour@hotmail.com"
